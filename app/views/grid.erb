<% @cohorts.each_with_index do |cohort, cohort_index| %>
<div class="cohort-container">
    <div class="cohort-title"><%= cohort.name + "  -  " + cohort.graduation_date.strftime('%B %-d, %Y') %></div>
    <div class="cohort c<%= cohort_index %>">
        <% cohort.users.each do |user| %>
        <div class="user" data-id=<%= user.id %> data-lineage=<%= user.lineage_id %> ><img src="<%= user.primary_profile_url %>" /><div class="name"><%= user.name %></div></div>
        <% end %>
    </div>
</div>
<% end %>
