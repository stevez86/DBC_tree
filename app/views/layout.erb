<!DOCTYPE html>
<html lang="en">
<head>
  <!-- 
    normalize.css removes cross-browser differences in defaults, e.g.,
    differences in how form elements appear between Firefox and IE
    See: http://necolas.github.com/normalize.css/
  -->
  <link rel="stylesheet" href="/css/normalize.css">
  <!--
    application.css is where you put your styles
  -->
  <link rel="stylesheet" href="/css/application.css">

  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
  <script src="/js/application.js"></script>

  <title></title>

  <div class="heading">
    <form action="/user/<%= @user.id %>/timeline" method="get">
      <button class="headbutton">Timeline</button>
    </form>
    <form action="/user/<%= @user.id %>" method="get">
      <button class="headbutton">Profile</button>
    </form>
    <form action="/user/<%= @user.id %>/followers" method="get">
      <button class="headbutton">Followers</button>
    </form>
    <form action="/user/<%= @user.id %>/following" method="get">
      <button class="headbutton">Following</button>
    </form>
      <button class="signout">Sign Out</button>
  </div>
</head>
<body>
  <%= yield %>
</body>
</html>
